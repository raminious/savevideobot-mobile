import React from 'react'
import { Icon, ListItem, Body, Left, Right, Text } from 'native-base'

export default ({

}) => (
  <ListItem icon>
    <Left>
      <Icon name="timer" />
    </Left>
    <Body>
      <Text note>Subscription</Text>
    </Body>
    <Right>

    </Right>
  </ListItem>
)
