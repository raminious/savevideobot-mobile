import React from 'react'
import { ActivityIndicator } from 'react-native'
import { Header, Text, Icon, Title, Right, Body } from 'native-base'

export default () => (
  <Header>
    <Body>
      <Title>SaveVideBot Live</Title>
    </Body>
  </Header>
)
